export class Player {
  constructor(
    public id: number,
    public email: string,
    public name: string,
    public password: string,
    public bio: string,
    public portfolio: Number
    //add picture, bio, email
  ) {  }
}
